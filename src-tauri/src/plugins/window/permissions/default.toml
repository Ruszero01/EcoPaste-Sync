"$schema" = "schemas/schema.json"

[default]
description = "Default permissions for the window plugin"
permissions = [
    "allow-toggle-window",
    "allow-show-taskbar-icon",
    "allow-apply-mica-effect",
    "allow-clear-mica-effect",
    "allow-is-mica-supported",
    "allow-create-window"
]
